[package]
name = "hajiz"
version = "0.1.0"
edition = "2021"
description = "Secure application sandboxing system in Rust"
license = "MIT"

[dependencies]
thiserror = "1.0"
nix     = { version = "0.29", features = ["signal", "process", "sched", "mount"] }
libseccomp = "0.3"

[dev-dependencies]
